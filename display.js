/* ==========================================================================
   display.js — CHUNK 5

   Purpose: wires up the Public Display page — renders the current ticket
   in NOW SERVING, triggers the bell + flash animation + speech.js
   announcement on new tickets, keeps the last-5 list and clock updated,
   runs the scrolling footer, and handles fullscreen + Wake Lock so the
   screen doesn't sleep during idle periods.

   Depends on: comms.js, state.js, settings.js, speech.js.
   Depended on by: nothing (top-level page script).
   ========================================================================== */

console.log("[display.js] loaded — Chunk 5 not yet implemented");
