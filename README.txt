Put these here before Chunk 5/6:

  bell.mp3              — short notification sound, plays on each new call
  logo-placeholder.png  — default logo shown until operator uploads one
                           in Settings (Chunk 3)

Keep bell.mp3 short (under ~2 seconds) and small in file size — it gets
loaded on every page load of display.html.
